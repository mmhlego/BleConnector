namespace BleConnector.Utils {
    /// <summary>
    /// List of echo modes describing the behavior of mintti stethoscope
    /// </summary>
    public enum EchoMode {
        MODE_BELL_ECHO,
        MODE_DIAPHRAGM_ECHO
    }
}
